---
- name: bgp c'est de la merde je deteste ansible je veux mourir
  hosts: all
  remote_user: admin

  tasks:
  - name: bgp c'est de la merde je deteste ansible je veux mourir
    bgp3:
      yaml_file: ./bgp_config.yml
      jinja2_template: ./bgp_template.j2
      host: 192.168.100.254
      username: admin
      password: admin
      secret: admin